# Custom keyboard shortcuts to spawn apps
#
# Docs can be found here:
# https://github.com/koekeishiya/skhd

# open vscode:
ctrl - p : open -a 'Cursor'

# open browser:
ctrl - o : open -a 'Firefox'

# open telegram:
ctrl - t : open -a 'Telegram Desktop'

# open Chrome:
ctrl - y : open -a "Google Chrome" --args --profile-directory="Default"

.blacklist [
    "iTerm"
    "terminal"
]

# Restart skhd service `skhd --restart-service`
